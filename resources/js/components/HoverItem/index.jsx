import React from 'react';

import './style.scss';

const HoverItem = (props) => {
    return (
        <>
            <span>{props.contentCut}</span>
        </>
    );
};

export default HoverItem;
